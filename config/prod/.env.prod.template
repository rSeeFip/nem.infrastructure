# === Database ===
# PostgreSQL superuser name for infrastructure bootstrap.
POSTGRES_SUPERUSER=${POSTGRES_SUPERUSER}
# PostgreSQL superuser password for production bootstrap.
POSTGRES_SUPERPASS=${POSTGRES_SUPERPASS}
# nem.MCP database user.
NEMMCP_DB_USER=${NEMMCP_DB_USER}
# nem.MCP database password.
NEMMCP_DB_PASS=${NEMMCP_DB_PASS}
# KnowHub database user.
KNOWHUB_DB_USER=${KNOWHUB_DB_USER}
# KnowHub database password.
KNOWHUB_DB_PASS=${KNOWHUB_DB_PASS}
# Mimir database user.
MIMIR_DB_USER=${MIMIR_DB_USER}
# Mimir database password.
MIMIR_DB_PASS=${MIMIR_DB_PASS}
# Classification database user.
CLASSIFICATION_DB_USER=${CLASSIFICATION_DB_USER}
# Classification database password.
CLASSIFICATION_DB_PASS=${CLASSIFICATION_DB_PASS}
# Comms database user.
COMMS_DB_USER=${COMMS_DB_USER}
# Comms database password.
COMMS_DB_PASS=${COMMS_DB_PASS}
# Inference service database user.
INFERENCE_DB_USER=${INFERENCE_DB_USER}
# Inference service database password.
INFERENCE_DB_PASS=${INFERENCE_DB_PASS}

# === RabbitMQ ===
# RabbitMQ username.
RABBITMQ_USER=${RABBITMQ_USER}
# RabbitMQ password.
RABBITMQ_PASS=${RABBITMQ_PASS}

# === Keycloak ===
# Keycloak admin username.
KEYCLOAK_ADMIN=${KEYCLOAK_ADMIN}
# Keycloak admin password.
KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD}
# OIDC authority URL for the nem realm.
KEYCLOAK_AUTHORITY=${KEYCLOAK_AUTHORITY}
# Expected token audience.
KEYCLOAK_AUDIENCE=${KEYCLOAK_AUDIENCE}
# Whether HTTPS is required when validating tokens.
KEYCLOAK_REQUIRE_HTTPS=${KEYCLOAK_REQUIRE_HTTPS}

# === OpenBao ===
# OpenBao base address.
OPENBAO_ADDR=${OPENBAO_ADDR}
# OpenBao root/service token.
OPENBAO_TOKEN=${OPENBAO_TOKEN}
# KV secrets engine mount point.
OPENBAO_MOUNT_POINT=${OPENBAO_MOUNT_POINT}
# Transit secrets engine mount point.
OPENBAO_TRANSIT_MOUNT_POINT=${OPENBAO_TRANSIT_MOUNT_POINT}

# === OPA ===
# OPA API base URL.
OPA_BASE_URL=${OPA_BASE_URL}

# === LLM Providers ===
# Local LM Studio OpenAI-compatible endpoint.
LMSTUDIO_BASE_URL=${LMSTUDIO_BASE_URL}
# LiteLLM OpenAI-compatible endpoint.
LITELLM_BASE_URL=${LITELLM_BASE_URL}
# GitHub Copilot API endpoint.
COPILOT_API_ENDPOINT=${COPILOT_API_ENDPOINT}
# GitHub Copilot API key.
COPILOT_API_KEY=${COPILOT_API_KEY}

# === Gateway ===
# Gateway listener port.
GATEWAY_PORT=${GATEWAY_PORT}
# Grafana admin password.
GF_ADMIN_PASSWORD=${GF_ADMIN_PASSWORD}

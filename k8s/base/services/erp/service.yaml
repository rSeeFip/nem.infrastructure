apiVersion: v1
kind: Service
metadata:
  name: nem-erp
  namespace: nem
  labels:
    app: nem-erp
    app.kubernetes.io/name: nem-erp
    app.kubernetes.io/part-of: nem
spec:
  type: ClusterIP
  selector:
    app: nem-erp
  ports:
    - name: http
      port: 8080
      targetPort: http

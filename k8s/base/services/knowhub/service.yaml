apiVersion: v1
kind: Service
metadata:
  name: nem-knowhub
  namespace: nem
  labels:
    app: nem-knowhub
    app.kubernetes.io/part-of: nem-platform
spec:
  type: ClusterIP
  selector:
    app: nem-knowhub
  ports:
    - port: 5001
      targetPort: 5001
      protocol: TCP

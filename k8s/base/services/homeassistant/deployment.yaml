apiVersion: apps/v1
kind: Deployment
metadata:
  name: nem-homeassistant
  namespace: nem
  labels:
    app: nem-homeassistant
    app.kubernetes.io/part-of: nem-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nem-homeassistant
  template:
    metadata:
      labels:
        app: nem-homeassistant
        app.kubernetes.io/part-of: nem-platform
    spec:
      containers:
        - name: nem-homeassistant
          image: localhost:5000/nem-homeassistant:dev
          ports:
            - containerPort: 5800
          envFrom:
            - configMapRef:
                name: nem-homeassistant-config
          readinessProbe:
            httpGet:
              path: /health
              port: 5800
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m

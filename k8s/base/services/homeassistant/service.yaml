apiVersion: v1
kind: Service
metadata:
  name: nem-homeassistant
  namespace: nem
  labels:
    app: nem-homeassistant
    app.kubernetes.io/part-of: nem-platform
spec:
  type: ClusterIP
  selector:
    app: nem-homeassistant
  ports:
    - port: 5800
      targetPort: 5800
      protocol: TCP

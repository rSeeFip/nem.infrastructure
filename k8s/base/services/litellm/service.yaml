apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: nem
  labels:
    app: litellm
    app.kubernetes.io/part-of: nem-platform
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP

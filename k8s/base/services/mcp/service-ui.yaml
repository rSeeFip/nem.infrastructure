apiVersion: v1
kind: Service
metadata:
  name: nem-mcp-ui
  namespace: nem
  labels:
    app: nem-mcp-ui
    app.kubernetes.io/part-of: nem-platform
spec:
  type: ClusterIP
  selector:
    app: nem-mcp
  ports:
    - port: 4200
      targetPort: 8080

apiVersion: v1
kind: Service
metadata:
  name: nem-mcp
  namespace: nem
  labels:
    app: nem-mcp
    app.kubernetes.io/part-of: nem-platform
spec:
  type: ClusterIP
  selector:
    app: nem-mcp
  ports:
    - port: 8080
      targetPort: 8080

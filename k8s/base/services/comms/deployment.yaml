apiVersion: apps/v1
kind: Deployment
metadata:
  name: nem-comms
  namespace: nem
  labels:
    app: nem-comms
    app.kubernetes.io/part-of: nem-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nem-comms
  template:
    metadata:
      labels:
        app: nem-comms
        app.kubernetes.io/part-of: nem-platform
    spec:
      containers:
        - name: nem-comms
          image: localhost:5000/nem-comms:dev
          ports:
            - containerPort: 5280
          envFrom:
            - configMapRef:
                name: nem-comms-config
          env:
            - name: NemConfiguration__ClientSecret
              valueFrom:
                secretKeyRef:
                  name: nem-comms-configuration-secret
                  key: NemConfiguration__ClientSecret
          readinessProbe:
            httpGet:
              path: /health
              port: 5280
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m

apiVersion: v1
kind: Service
metadata:
  name: openbao
  namespace: nem
  labels:
    app: openbao
    app.kubernetes.io/part-of: nem-platform
spec:
  type: ClusterIP
  selector:
    app: openbao
  ports:
    - name: http
      port: 8200
      targetPort: http

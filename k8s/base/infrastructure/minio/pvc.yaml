apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: nem
  labels:
    app: minio
    app.kubernetes.io/name: minio
    app.kubernetes.io/part-of: nem
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

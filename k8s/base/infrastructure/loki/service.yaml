apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: nem
  labels:
    app: loki
    app.kubernetes.io/part-of: nem-platform
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3100
    targetPort: 3100
    protocol: TCP
  selector:
    app: loki

apiVersion: v1
kind: Service
metadata:
  name: opa
  labels:
    app: opa
    app.kubernetes.io/part-of: nem-platform
spec:
  type: ClusterIP
  selector:
    app: opa
  ports:
    - port: 8181
      targetPort: 8181

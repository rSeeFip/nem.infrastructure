apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tempo-data
  namespace: nem
  labels:
    app: tempo
    app.kubernetes.io/part-of: nem-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

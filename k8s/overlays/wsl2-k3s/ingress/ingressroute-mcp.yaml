apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute-mcp
  namespace: nem
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`mcp.nem.local`)
      kind: Rule
      services:
        - name: nem-mcp
          port: 8080

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute-gateway
  namespace: nem
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`gateway.nem.local`)
      kind: Rule
      services:
        - name: gateway
          port: 8090

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute-mcp-ui
  namespace: nem
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`mcp-ui.nem.local`)
      kind: Rule
      services:
        - name: nem-mcp-ui
          port: 4200

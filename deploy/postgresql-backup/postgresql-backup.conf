# Installed as /etc/nem/postgresql-backup.conf (root:root 0644).
# Contains no credentials. Production paths are immutable in common.sh.
BACKUP_ENABLED=true
RETENTION_KEEP=7
FRESHNESS_MAX_SECONDS=129600
MAX_RATE=32M
